/**
 * StrategyDashboard — multi-slot strategy view with competitor heatmap.
 * Sprint 11: implement slot heatmap, competitor comparison, strategy export.
 */

import React from "react";

export default function StrategyDashboard() {
  return (
    <div>
      <h2>Strategy Dashboard</h2>
      <p style={{ color: "#666" }}>
        Sprint 11: slot ranking table, competitor heatmap, pattern alerts, strategy export.
      </p>
    </div>
  );
}
