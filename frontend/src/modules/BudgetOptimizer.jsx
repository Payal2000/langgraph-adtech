/**
 * BudgetOptimizer — UI for budget allocation queries.
 * Sprint 11: implement slot picker, budget slider, allocation table.
 */

import React from "react";

export default function BudgetOptimizer() {
  return (
    <div>
      <h2>Budget Optimizer</h2>
      <p style={{ color: "#666" }}>
        Sprint 11: budget allocation UI — slot picker, budget slider, ROI projections, pacing status.
      </p>
    </div>
  );
}
