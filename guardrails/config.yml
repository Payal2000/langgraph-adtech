models:
  - type: main
    engine: bedrock
    model: anthropic.claude-3-sonnet-20240229-v1:0

rails:
  input:
    flows:
      - check bid limits
      - check category blocks
  output:
    flows:
      - check response compliance

instructions:
  - type: general
    content: |
      You are a compliance layer for an ad bidding AI system.
      Flag any request that violates bid limits or category restrictions.
      Do not block legitimate bidding strategy questions.
